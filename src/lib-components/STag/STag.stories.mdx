import { action } from '@storybook/addon-actions'
import { config, withDesign } from 'storybook-addon-designs'
import { withTests } from '@storybook/addon-jest';
import {
  Meta,
  Story,
  ArgsTable,
  Description,
  Source,
  Props,
  Preview,
} from '@storybook/addon-docs'
import results from '/.jest-test-results.json';
import STag from '@/lib-components/STag/STag.vue'

<Meta
  title="DESIGN SYSTEM/Atom/Tag"
  component={STag}
  decorators={[withDesign, withTests({results})]}
  argTypes={{
    bgColor: { control: 'text' },
    textColor: { control: 'text' },
    label: { control: 'text' },
  }}
  parameters={{
    design: config({
      type: 'figma',
      url:
        'https://www.figma.com/file/42Xh2qI3qvq2CRkc2HJlwA/MVP-3-Market-Catalog-%E2%80%A2-Product-Page?node-id=1128%3A88',
    }),
    docs: {
      source: {
        type: 'code',
      },
    },
  }}
/>

# Тег

<Description of={STag} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { STag },
  template:
    `<s-tag :bgColor="bgColor" :textColor="textColor" :label="label" />`,
})

<Preview withToolbar withSource="none">
  <Story name="STag" args={{ textColor: 'diamond', label: 'Возможна резка' }}>
    {Template.bind({})}
  </Story>
</Preview>

## Пример

<Source
  language="html"
  code={`
    <s-tag bg-color="diamond-light" text-color="diamond" label="Возможна резка" />
  `}
/>

<ArgsTable of={STag} />

<div style={{padding: '10px 0', marginBottom: '15px'}}>
    <span style={{color: '#111111', fontWeight: 700, fontSize: '20px'}}>Все значения цветов пишутся классом, если не указывать цвет фона, то он автоматически выставится как цвет текста с прозрачностью 0.08 </span>
</div>
