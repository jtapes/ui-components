import { config, withDesign } from 'storybook-addon-designs'
import { withTests } from '@storybook/addon-jest';
import {
  Meta,
  Story,
  ArgsTable,
  Description,
  Source,
  Canvas,
} from '@storybook/addon-docs'
import results from '/.jest-test-results.json';
import SBreadcrumbs from '@/lib-components/SBreadcrumbs/SBreadcrumbs.vue'

<Meta
  title="DESIGN SYSTEM/Atom/Breadcrumbs"
  component={SBreadcrumbs}
  decorators={[withDesign, withTests({results})]}
  argTypes={{
    breadcrumbs: {
      control: 'array',
      defaultValue: [
        { label: 'Каталог', link: 'catalog' },
        { label: 'Сортовой прокат', link: 'sortovoy-prokat' },
        { label: 'Лист горячетканый горячетканый горячетканый' },
      ]
    },
  }}
  parameters={{
    design: config({
      type: 'figma',
      url:
        'https://www.figma.com/file/42Xh2qI3qvq2CRkc2HJlwA/MVP-3-Market-Catalog-%E2%80%A2-Product-Page?node-id=9%3A284',
    }),
    docs: {
      source: {
        type: 'code',
      },
    },
  }}
/>

# Хлебные крошки

<Description of={SBreadcrumbs} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { SBreadcrumbs },
  template: `<s-breadcrumbs :breadcrumbs="breadcrumbs" />`,
})

<Canvas withToolbar withSource="none">
  <Story name="SBreadcrumbs">
    {Template.bind({})}
  </Story>
</Canvas>

## Пример

<Source
  language="html"
  code={`
<s-breadcrumbs :breadcrumbs="[{ label: 'Каталог', link: 'catalog' }, { label: 'Сортовой прокат', link: 'sotrovoy-prokat' }, { label: 'Лист горячетканый горячетканый горячетканый' }]">
  <template #separator>
    <span>-></span>
  </template>
</s-breadcrumbs>
  `}
/>

<ArgsTable of={SBreadcrumbs} />
